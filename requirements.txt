# Adaptive Cognitive Soundscape — MTX conda env dependencies
# Install with: pip install --no-user -r requirements.txt -e .
# 6.11.x can fail Qt DLL load on some Windows/conda setups (WinError 127).
PyQt6==6.8.1
PyQt6-Qt6==6.8.2
PyQt6-sip==13.10.0
pywin32==311
pynput==1.8.2
psutil==7.2.2
sounddevice==0.5.5
numpy==2.2.6
scipy==1.15.3
miniaudio==1.61
pydantic==2.10.4
pydantic-core==2.27.2
pydantic-settings==2.7.1
annotated-types==0.7.0
typing-extensions==4.15.0
python-dotenv==1.2.2
PyYAML==6.0.3
cffi==2.0.0
six==1.17.0
pytest==9.0.3
# Optional heavy dep: only needed for phrase-boundary feature precompute on
# newly uploaded songs (librosa.pyin + RMS ticks). Without it, uploaded songs
# skip precompute and simply fall back to the 5 s crossfade.
librosa>=0.11.0
